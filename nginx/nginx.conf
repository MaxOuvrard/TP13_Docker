upstream api_all {
    server cat:3000;
    server dog:3000;
}

server {
    listen 80;

    location / {
        proxy_pass http://api_all;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /cat {
        proxy_pass http://cat:3000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /dog {
        proxy_pass http://dog:3000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
